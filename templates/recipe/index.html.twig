{% extends 'base.html.twig' %}

{% block title %}Toutes Les recettes{% endblock %}

{% block body %}
    <p>
    Lorem ipsum
    </p>
    <ul>
        {% for recipe in recipes %}
        <li>
            <a href="{{ path('recipe.show', {id: recipe.id, slug: recipe.slug}) }}">{{ recipe.getTitle() }}</a>
        </li>
        {% endfor %}
    </ul>
{% endblock %}
